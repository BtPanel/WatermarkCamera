# 水印相机应用说明

## 1. 应用概述

水印相机是一款原生 Android 拍照与图片标注工具，面向外勤签到、工程记录、巡检取证和日常照片整理。它既可以在拍照前预览并调整水印，也可以导入相册照片后继续编辑。

| 项目 | 内容 |
| --- | --- |
| 应用名称 | 水印相机 |
| 版本 | 1.0（versionCode 1） |
| 包名 | `com.watermarkcamera.studio` |
| 最低系统 | Android 8.0（API 26） |
| 目标 SDK | Android 16 / API 36 |
| 屏幕方向 | 竖屏 |

## 2. 拍照与照片处理

- CameraX 相机支持前后摄像头、闪光灯、点击对焦、倒计时和焦距调节。
- 相机取景、照片方向和水印坐标固定为竖屏；横持设备时仅控制图标随方向旋转。
- 可通过 Android 系统相册选择照片，并保留当前模板继续编辑。
- 底图支持双指缩放和拖动，也可通过照片工具栏精确缩放或复位；结果会写入草稿和最终导出图。
- 支持自由裁剪、常用比例裁剪、专用模板比例和照片旋转。
- 成品以 JPEG 保存到系统相册，并可调用 Android 系统分享面板。

## 3. 水印与图层

应用支持文字、动态时间、位置、图片/Logo 和内置图标水印。文字、时间与位置默认使用透明背景。

新增水印后会立即选中。拖动水印主体可移动图层，拖动四角可等比例缩放，拖动边缘可单独调整宽高；双指手势可缩放和旋转。详细属性中还可以精确修改透明度、字号、尺寸、角度、颜色和字体。

文字类水印支持默认、窄体、衬线、等宽、手写字体，以及从手机存储导入自定义字体。颜色既可从常用色中选择，也可以输入 `#RRGGBB` 值。

图层操作包括锁定、隐藏、复制、删除、上下层级、画布对齐、撤销和重做。选中水印后显示可移动、可左右吸附的竖向工具栏，内容、外观、变换和图层面板按需展开。

图片水印和相册图片由 Android 系统解码器读取，实际格式支持范围取决于 Android 版本和设备。项目已验证 JPEG、PNG、GIF 和 BMP；动态图按静态首帧处理，不支持 SVG 水印。

## 4. 模板

应用提供时间地点、工程、巡检、外勤和生活等内置模板。用户可以把当前图层布局保存为命名模板，并进行调用、更新、重命名和删除。模板可以通过 JSON 文件导出到用户选择的位置，也可以重新导入。

模板会保存图层内容、位置、尺寸、透明度、旋转和样式。内置模板只是布局示例；与模板视觉风格可能涉及的第三方产品或品牌不存在隶属或授权关系。

基本流程：

1. 在相机页选择模板，或从空白画面开始添加水印。
2. 在取景区调整水印，随后拍照；也可以从相册导入照片。
3. 在编辑页裁剪照片并完成图层调整。
4. 保存到系统相册、分享图片，或把当前布局保存为新模板。

## 5. 位置与高德地图

位置水印支持三种输入方式：

1. 获取设备定位并进行逆地理编码。
2. 使用高德地图搜索地点并选择结果。
3. 手动输入或修改位置文字。

高德 Android Key 可以在应用“设置”中填写并保存在本机，也可以在项目根目录的 `gradle.properties` 中提供构建时备用值：

```properties
AMAP_API_KEY=你的高德AndroidKey
```

应用设置中的 Key 优先级更高；清空该设置后才会使用构建时备用 Key。高德开放平台中的 Android Key 应绑定包名 `com.watermarkcamera.studio`，并配置与安装包一致的签名 SHA-1。Debug 与 Release 签名通常不同，需要分别登记。

首次使用位置能力时，应用会先显示隐私提示；用户同意后才初始化相关 SDK。拒绝位置权限后，仍可编辑照片、搜索地点或手动输入位置文字。

## 6. 权限、隐私与本地数据

| 权限 | 用途 |
| --- | --- |
| 相机 | 取景与拍照 |
| 精确/大致位置 | 获取当前位置 |
| 网络、网络状态、Wi-Fi 状态 | 高德定位与地点搜索 |

模板、设置、草稿和应用内保存的高德 Key 保存在设备本地。导出图片进入系统相册；模板备份由用户通过系统文件选择器指定位置。应用核心的相册编辑与手动位置输入不依赖位置权限。

## 7. 品牌图标

版本 1.0 采用 B2 Coordinate Cut / R2 Signal Red 图标。图形将记录框、镜头/坐标点和右下切角组合，避免使用常见地图图钉轮廓。配色为：

- 信号红 `#E43D36`
- 白色 `#FFFFFF`
- 琥珀色 `#FFC247`

Android 自适应图标前景经过启动器实机/模拟器标定，白色主体约占最终图标宽度 60%。完整矢量源文件、PNG 多尺寸导出、清单、测试、导出脚本和设计来源说明保存在 `branding/app-icon/`，这些文件属于项目源资产，不是可清理的构建产物。

## 8. 开发、测试与发布

使用 Android Studio 打开项目根目录，安装 Android SDK 36，并使用 JDK 17。Windows PowerShell 构建示例：

```powershell
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:testDebugUnitTest
.\gradlew.bat :app:assembleDebug
```

Release 构建读取项目根目录的 `keystore.properties`：

```properties
storeFile=signing/你的密钥文件.jks
storePassword=你的密码
keyAlias=你的别名
keyPassword=你的密码
```

```powershell
.\gradlew.bat :app:assembleRelease
```

发布前至少应完成以下检查：

- 单元测试全部通过。
- APK 的 versionName 为 `1.0`、versionCode 为 `1`。
- 使用 Android `apksigner` 验证签名。
- 在真机验证相机方向、相册导入、裁剪、模板、定位和导出。
- 记录发布 APK 的 SHA-256 校验值。

## 9. 备份与敏感信息

可交付备份应包含干净源码、`branding/app-icon/`、最终 Release APK、校验值和备份说明，但不得包含以下内容：

- `.git/` 历史记录。
- `.gradle/`、`.kotlin/`、`build/`、`app/build/` 等构建缓存或产物目录。
- `verification/`、模拟器截图和旧 APK。
- `local.properties` 中的本机 Android SDK 路径。
- `keystore.properties`、`signing/` 中的私钥或密码。
- `gradle.properties` 中的真实 `AMAP_API_KEY`。

签名材料应只保存在受控的本机或密码管理/密钥管理系统中。丢失发布密钥可能导致后续版本无法覆盖安装，因此清理项目时不得删除本机签名文件。
